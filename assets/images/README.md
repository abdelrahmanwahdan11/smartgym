# Image Placeholders

This directory intentionally contains only text placeholders. The application renders illustrative graphics at runtime, so no binary assets are required. When integrating real media, replace these notes with optimized images.
